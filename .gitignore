# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Secrets — real .env is never committed; .env.example (no secrets) is.
.env
!.env.example

# Runtime state (snapshots, artifacts, FRED cache, sqlite) — never committed.
# Note: anchored to repo root so it does NOT match src/atlas/domain/data/.
/var/
/data/

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/

# Frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/
# Built SPA served by FastAPI at /app — produced by `pnpm build`, not committed.
src/atlas/interfaces/api/spa/
